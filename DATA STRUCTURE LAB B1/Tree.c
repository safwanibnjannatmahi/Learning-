#include <stdio.h>
#include <std>

//definr the structure for tree node 
struct Node;
{
    int data ;
    struct Node* left;
    struct Mode* right;
};

//Function to create a new node
struct Node* createNode (int data);
{
    struct Node*newNode = (struct Node*)malloc(sizeof())
}